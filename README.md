# Document-scanner

---

### Verification
- **README.md**: Matches your structure with setup instructions, code files, and test data.
- **Code Files**: All listed files are implemented and provided in prior responses.
- **Test Data**: Sample documents are included as `.txt` files for testing.

Place everything in a `document-scanner` folder, follow the `README.md` setup, and you’re ready to run the system:
```bash
npm install
npm start
